
$('.product-feed').append('<%= j render @products %>');
<% if @products.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @products %>');
<% else %>
  $('.layout').off('scroll');
  $('.pagination').remove();
<% end %>
