$('.product-feed').append('<%= j render 'users/my_picks', picks: @picks %>');
<% if @picks.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @picks %>');
<% else %>
  $('.layout').off('scroll');
  $('.pagination').remove();
<% end %>
