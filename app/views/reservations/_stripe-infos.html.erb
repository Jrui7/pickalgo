<div class="info-detail">Vous n'avez rien à payer aujourd'hui, mais vous vous engagez à ne pas vous désister si nous arrivons à obtenir un prix inférieur à celui mentionné dans le récapitulatif ci-contre.
</div>
<% if @customer_infos.present? %>
  <p>Ma carte</p>
<% else %>
  <%= form_tag pick_reservations_path(@pick), id: "reservation-form" do %>
    <div class="form-row">
      <label for="card-element">
        Carte
      </label>
      <div id="card-element">
        <!-- A Stripe Element will be inserted here. -->
      </div>

      <!-- Used to display form errors. -->
      <div id="card-errors" role="alert"></div>
    </div>
    <div class="validate-form-button">
      <button class="booking-button btn-orange" id="validate-reservation">
        <i class="fa fa-lock"></i>
        <span>Confirmer réservation</span>
    </button>
    </div>
  <% end %>
<% end %>
